{
  "name": "evaluate-value",
  "description": "Return a value or an evaluated function (with arguments).",
  "version": "1.0.2",
  "license": "MIT",
  "author": "Steven Vachon <contact@svachon.com> (https://www.svachon.com/)",
  "main": "lib",
  "repository": "stevenvachon/evaluate-value",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.0.0",
    "mocha": "^3.4.2"
  },
  "engines": {
    "node": ">= 4"
  },
  "scripts": {
    "pretest": "babel src --out-dir=lib --presets=es2015",
    "test": "mocha test --reporter spec --check-leaks --bail"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "function",
    "options",
    "value"
  ]
}
